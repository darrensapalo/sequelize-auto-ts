export default {
    /**
     * Determines where the model files should be parsed from.
     * 
     * Note that this tool does not perform a recursive 
     * transformation for each model file in subfolders.
     */
    "folderPath": "/folder/to/models/"
}